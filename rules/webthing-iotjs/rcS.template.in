# TODO: sed base_url
echo "# Network"
ifconfig
ifup eth0
renew eth0
ifconfig
sleep 1

#mkdir /tmp; mount -t tmpfs /tmp ; cd /tmp 
#sleep 1
#cd /tmp/
echo "# TODO: IOTJS_PATH=/mnt/sdcard"

mkdir /mnt
mount -t tmpfs /mnt/ ; cd /mnt

cd /rom
iotjs index
